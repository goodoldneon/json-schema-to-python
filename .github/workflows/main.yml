on: [push]
jobs:
  utest:
    runs-on: ubuntu-20.04
    # strategy:
    #   matrix:
    #     python-version: ["3.10"]
    steps:
      - name: Set up Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.10'
      - name: Print Python version
        run: python --version
